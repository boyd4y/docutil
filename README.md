docutil command util help launch and build docker develop image easily!

see help with docutil -h

docutil follow before rule to name local image.
<current dir>/<git branch>

also, build run will optimize the common usage by adding following parameter for dev usage.

--rm, remove image after terminated.
-v, mount current dir to /src (you can specify in configuration or parameter TBD)

